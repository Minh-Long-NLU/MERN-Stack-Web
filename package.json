{
    "name": "cdweb-project",
    "version": "1.0.0",
    "scripts": {
      "postinstall": "cd client && npm install && npm run build && cd ../api && npm install",
      "start": "node api/index.js"
    },
    "dependencies": {
      "concurrently": "^5.0.0"
    }
  }
  